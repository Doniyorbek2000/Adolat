/**
 * Sahifalash tiplari `@adolat/shared` paketidan re-export qilinadi, shunda
 * backend, admin va mobil ilova bir xil kontraktdan foydalanadi. Mavjud
 * importlar (`./pagination.types`) o'zgarishsiz ishlashda davom etadi.
 */
export type {
  PaginationQuery,
  PaginationMeta,
  PaginatedResult,
} from '@adolat/shared';
