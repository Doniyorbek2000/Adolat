{
  "name": "adolat-ai",
  "private": true,
  "version": "0.1.0",
  "description": "Adolat AI — O'zbekiston bozori uchun huquqiy AI platformasi (mobile + backend + admin)",
  "workspaces": [
    "apps/api",
    "apps/admin",
    "packages/shared"
  ],
  "scripts": {
    "shared:build": "npm run build --workspace=packages/shared",
    "api:dev": "npm run start:dev --workspace=apps/api",
    "api:build": "npm run shared:build && npm run build --workspace=apps/api",
    "admin:dev": "npm run dev --workspace=apps/admin",
    "admin:build": "npm run shared:build && npm run build --workspace=apps/admin",
    "build": "npm run shared:build && npm run build --workspace=apps/api && npm run build --workspace=apps/admin",
    "test": "npm run test --workspace=apps/api",
    "lint": "npm run lint --workspace=apps/api && npm run lint --workspace=apps/admin"
  },
  "engines": {
    "node": ">=20"
  }
}
