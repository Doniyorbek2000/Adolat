{
  "name": "@adolat/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Adolat AI — umumiy tiplar, enum'lar, konstantalar (backend + admin + mobil kontraktlari)",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "typescript": "^5.5.4"
  }
}
